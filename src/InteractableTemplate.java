public interface InteractableTemplate {
    //The description
    public String InteractableDescription();

    //List of things the player can look at
    //Maybe an array of locked elements and points of interest? (like the bear rug's mouth in the document)

    //(Other things)
}
